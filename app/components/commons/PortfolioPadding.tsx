import React from 'react';

const PortfolioPadding = ({ children }: { children: React.ReactNode }) => {
  return <div className="xl:w-[1300px] m-auto ">{children}</div>;
};

export default PortfolioPadding;
